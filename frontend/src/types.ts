export type TypeMenu = {
    id: number;
    title: string;
    url: string;
    serial_number: number;
};

export type TypeSocialItems = {
    id: number;
    title: string;
    svg: JSX.Element;
    url: string;

};
